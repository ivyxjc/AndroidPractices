<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jc.viewabout" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >

        </activity>

        <activity android:name=".DetaiActivity">

        </activity>

        <activity android:name=".ViewAbout">

        </activity>

        <activity android:name=".customView.counterView.CounterActivity">

        </activity>

        <activity android:name=".customView.titleView.TitleViewActivity">

        </activity>
        
        <activity android:name=".customView.slideListView.SlideListViewActivity">

        </activity>

        <activity android:name=".viewUsage.SelectorAcitivty">

        </activity>

        <activity android:name=".customView.slideRecyclerView.SlideRecyclerViewActivity">

        </activity>

        <activity android:name=".customView.customOnDrawView.CustomOnDrawActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>